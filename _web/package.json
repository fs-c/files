{
  "scripts": {
    "dev": "cross-env dhow dev -i pages --disable-cache",
    "build": "dhow build -i pages"
  },
  "dependencies": {
    "byte-size": "^8.1.0",
    "date-fns": "^2.29.3",
    "fs-extra": "^10.1.0"
  },
  "devDependencies": {
    "@fsoc/dhow": "^1.2.2",
    "@tailwindcss/typography": "^0.5.7",
    "autoprefixer": "^10.4.12",
    "cross-env": "^7.0.3",
    "postcss": "^8.4.17",
    "tailwindcss": "^3.1.8"
  }
}
